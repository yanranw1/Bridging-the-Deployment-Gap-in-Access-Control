result_prefix: 'llama3_cyber'
dataset: '../../data/document_folds/cyber_acp.csv'
generation:
  model: "meta-llama/Meta-Llama-3-8B"
  checkpoint: "../../checkpoints/generation/cyber/checkpoint/"
  device: "cuda:0"
verification:
  enable: False
configs:
  save_detailed_results: True
  folder: 'results'